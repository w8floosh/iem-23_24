/* for (int i=0; i<height; i=i+10)  line (i, 3*i, 2*i+20, 2*i+20); 
ellipseMode(CORNER);
for(int i=0; i<width; i=i+10)
  for (int j=0; j<height; j=j+10)
    circle(i,j,10);
*/
    
    
/*void setup(){
  size(500, 500);
  background(255);
  frameRate(30);
  println("Non ho paura di nessuno tranne di quelli che conoscono a memoria le canzoni di Cremonini\n");
  ellipseMode(CORNER);
}

void draw(){
  println("Sono passati " + frameCount + " framesssssseeeess\n");
  color C = color(random(0,255), random(0,255), random(0,255)); 
  fill(C, random(0,255));
  //if (frameCount>=200)  circle(random(0, width), random(0, height), random(0,50));
  double var = millis() % 50;
  if (var<20 && var>0)  circle(random(0, width), random(0, height), random(0,50));
}*/

void setup(){
  size(750, 750);
  background(255);
}
void draw(){
  /*background(255);
  fill(140, 100, 2, 128);
  rect(0,0,width+5,height+5);
  fill(255,0,0);
  ellipse(mouseX, mouseY, 50,60);*/
  
  
  /*int a1x=100;
  int a1y=100;
  circle(a1x, a1y, 10);
  int c1x=300;
  int c1y=100;
  circle(c1x, c1y, 20);
  int c2x=300;
  int c2y=600;
  circle(c2x, c2y, 20);
  int a2x=100;
  int a2y=600;
  circle(a2x, a2y, 10);
  stroke(255, 0,0);
  line(a1x,a1y, c1x,c1y);
  line(a2x,a2y, c2x,c2y);
  //line(c1x,c1y, c2x,c2y);
  stroke(0);
  bezier(a1x, a1y, c1x, c1y, c2x, c2y, a2x, a2y);
  */
  
  
}
